#include <stdio.h>
#include <math.h>

/**
 * @brief Функция a принимает два аргумента x и y и возвращает вычисленное значение.
 *
 * @param x Первый аргумент.
 * @param y Второй аргумент.
 * @return Вычисленное значение по формуле (2 * cos(x - π/6)) / (0.5 + sin²(y)).
 */
double a(double x, double y);

/**
 * @brief Функция b принимает три аргумента x, y и z и возвращает вычисленное значение.
 *
 * @param x Первый аргумент.
 * @param y Второй аргумент.
 * @param z Третий аргумент.
 * @return Вычисленное значение по формуле.
 */
double b(double x, double y, double z);

// Константы для значений переменных
const double X = 1.426;
const double Y = -1.22;
const double Z = 3.5;

int main() {
    // Вывод значений переменных и результатов функций a и b
    printf("%lf\n%lf\n%lf\n%lf\n%lf\n", X, Y, Z, a(X, Y), b(X, Y, Z));

    return 0; // Завершаем программу с нулевым статусом
}

const double a(const double x, const double y) {
    // Вычисление значения по формуле
   
    return (2 * cos(x - (M_PI / 6))) / (0.5 + pow(sin(y), 2)) ; // Возвращаем вычисленный результат
}

const double b(const double x, const double y, const double z) {
    // Вычисление значения по формуле
 
    return (1 + pow(z, 2)) / (3 + (pow(z, 2) / 5)); // Возвращаем вычисленный результат
}
