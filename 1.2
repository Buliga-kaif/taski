
#include<stdio.h> 
#include<math.h> 
#include<stdlib.h>

/**
 * @brief Главная функция программы
 *
 * Данная программа вычисляет площадь грани, площадь полной поверхности и объем куба
 * на основе введенной длины его ребра. Результаты выводятся на экран.
 *
 * @return int
 */
double getFace_area(const double leght);

double geTtotal_surface_area(const double face_area);
double getVolume(const double leght);

int main(void) {
    float length;
    printf("leght:\t");
    const double leght = input();
    return 0;
}
double input (void){
    double number;
    if (!scanf_s("%lf", &number)) {
        printf("Input Error\n");
        exit(EXIT_FAILURE);
    }
    return number;
}
double getFace_area(const double leght) {
    const int ConversionFactor = 1000;
    return (leght*leght);
}
double geTtotal_surface_area(const double face_area) {
    const int ConversionFactor = 1000;
    return (6* face_area);
    }
double getVolume(const double leght) {
    const int ConversionFactor = 1000;
    return (pow(leght, 3));
    }
